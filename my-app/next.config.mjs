/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ['img.freepik.com',],
    domains: ['marketplace.canva.com',], // allow this domain
  },
};

export default nextConfig;
